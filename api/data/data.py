class Data:
    """Data is a class for deserialize a string into a python object"""
    def __init__(self, deductible, stop_loss, oop_max):
        self.deductible = deductible
        self.stop_loss = stop_loss
        self.oop_max = oop_max

